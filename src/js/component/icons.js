import fontawesom from "@fortawesome/fontawesome";
import faClock from "@fortawesome/fontawesome-free-regular/faClock";
fontawesome.config = {
    autoRepalceSvg: "nest"
}
fontawesome.library.add(faClock);